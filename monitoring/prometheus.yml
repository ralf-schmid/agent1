global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'losungsbot'
    static_configs:
      - targets: ['host.docker.internal:8080']
    # Falls host.docker.internal nicht funktioniert, alternative:
    # - targets: ['172.17.0.1:8080']  # Docker bridge IP
